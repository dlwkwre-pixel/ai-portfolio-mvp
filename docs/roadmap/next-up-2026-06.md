# Next-up queue (captured 2026-06-28)

User picked these four directions. Order: **site speed first**, then the rest.

## 1. Site speed / navigation latency 🟠 IN PROGRESS
The #1 high-leverage item before onboarding strangers.

### Shipped
- **Lazy-loaded `xlsx-js-style`** (5ac6feb): was statically imported into planning-client but only
  used on the Excel-export click. Dynamic-imported inside `exportForecastXLSX` → /planning's
  largest chunk dropped **1.34MB → 862KB** (xlsx now a separate on-demand ~474KB chunk).
- **Parallelized per-portfolio valuations** (b24a516) on dashboard, planning, tax, portfolios:
  were serial `for await getPortfolioValuation` loops (render waited on the SUM of every
  portfolio's market-data latency) → `Promise.all` bounds it to the slowest single portfolio.
  Dashboard also parallelizes its per-portfolio holdings fetch.

### Shipped (cont.)
- **One holdings query on dashboard** (was N round-trips): single `.in("portfolio_id", ids)` +
  group in memory, then parallel valuations.

### Findings (already handled / lower ROI than expected)
- **Quote fetches are already deduped/cached**: `getFinnhubQuote` uses `fetch(..., { next:
  { revalidate: 60 } })`, so Next request-memoizes identical same-ticker fetches within a render
  and caches them 60s across requests. The parallel valuations do NOT cause redundant network
  calls. No extra quote cache needed.
- **recharts is a SHARED vendor chunk** (~10 components import it) loaded once, not duplicated per
  route — and most charts render above-the-fold, so per-component `next/dynamic` lazy-load has
  modest ROI for real visual-regression risk. Deferred.

### Shipped (cont.)
- **Lazy-loaded Master Life Roadmap** (6f2408b): events-tab panel now `dynamic(ssr:false)`.
- **Extracted EstatePlanningTab** to its own lazy module (estate-planning-tab.tsx) — trims initial
  planning JS; loads only when the Estate tab opens.

### ⚠️ KEY CORRECTION — the "862KB chunk" is NOT the initial bundle
Verified by grepping the chunk: the 862KB "biggest chunk" contains **xlsx** (the Excel export
lib), not recharts/react. It's the chunk the xlsx lazy-load split out (5ac6feb) — a DEFERRED,
on-demand chunk that only downloads when a user clicks Export. It is NOT in the initial /planning
load. The scary headline number has been the parked Excel lib the whole time. So:
- The xlsx lazy-load was the decisive bundle win (deferred the single heaviest dep off the
  critical path).
- The real initial /planning JS = shared vendor chunks (~360KB, recharts+react, loaded on any
  chart route) + planning's own (smaller) app chunk. Extracting panels trims the app chunk ~25KB
  each — real but marginal, invisible behind the parked xlsx in the size ranking.
- CONCLUSION: per-panel extraction is now low-ROI (≈25KB each, refactor risk on the flagship).
  High-impact speed work is done (parallel fetches, single query, xlsx deferral, skeletons,
  prefetch). Further panel extraction = optional polish, not a real bottleneck.

### Investigated — planning-client split is a DEDICATED PROJECT (not a quick win)
Measured: the 862KB /planning chunk is `planning-client.tsx`'s OWN code (a ~5000-line main
component + ~21 top-level sub-component functions). Dynamic-importing the two already-separate
files (MasterLifeRoadmap, Plan401kSection) did NOT move the 862KB — they were already separate
chunks. To shrink it, each inline sub-component (EstatePlanningTab ~660 lines, OnboardingWizard,
CompareTab, BudgetTrackerTab, CashFlowOS, BalanceSheetOS, AiImportPanel, StatementImportPanel…)
must be MOVED to its own file + `dynamic()`-imported. Findings:
- The sub-components are top-level functions with clean props (no parent closures) + minimal
  shared deps (mostly a trivial `fmt` money formatter) → mechanically extractable, tsc-validated.
- BUT each is a 300-660 line block-move out of the flagship file; payoff ~30-50KB each, so it
  takes 6-8 extractions to meaningfully shrink → a multi-step refactor that should be done one
  component per commit WITH device testing between steps. High effort, modest per-step payoff,
  real regression risk if rushed.
RECOMMENDATION: do this as its own focused session, extracting one panel at a time (start with
EstatePlanningTab — estate-only helpers DOC_STATUSES/RELATIONSHIPS/statusColor/statusLabel at
lines 3182-3206 move with it; only shared dep is `fmt`). Verify build + the tab on device, commit,
repeat. Perceived "slow on click" is already largely addressed by parallel fetches + skeletons +
Link prefetch, so this is optimization, not a blocker.
- **recharts** is a SHARED vendor chunk (not in the 862KB planning chunk) — lazy-loading it
  doesn't shrink /planning. Lower priority.
- **Prefetch**: landing/auth use `<a>` (no prefetch); in-app uses `<Link>` (prefetches).

## 2. Atlas personality in Planning ✅ ALREADY DONE (verified 2026-06-28)
On inspection this was already built (the roadmap note was based on a stale memory). The planning
**Ask Atlas chat** is fully realized: Atlas avatar + online status, cycling contextual thinking
states + glowing orb, typewriter answer animation, domain-grouped suggested prompts, and a
financial-position snapshot. The **commentary** is Atlas-voiced too: Overview ("Atlas's Biggest
Insight" card + Ask-Atlas button), Balance ("Atlas strip"), and Cash Flow tabs all render insights
in Atlas micro-cards (orb + label). Sub-planner AI takes share atlas-thinking.tsx. No real gap —
MEMORY.md index corrected.

## 3. Gamification activation polish ✅ SHIPPED (04f8e58)
- **Instant bell**: createHolding + AI-run now fire `getWeeklyChallenges` immediately, so the
  challenge-complete notification lands the moment you finish (not just on next page load).
- **Level-up celebration**: global `LevelUpWatcher` (app/components/level-up-watcher.tsx) detects
  a level increase vs the last level seen on the device and shows a confetti celebration.
- **Dashboard widget**: app/dashboard/challenges-widget.tsx shows the level/XP bar + active weekly
  quests with done state, fetched client-side (non-blocking) from new /api/me/gamification.
- FUTURE (optional): also wire the bell into community-post + strategy-create actions; fix the
  `assign_strategy` quest (its xp_events kind `strategy_assigned` is never awarded → currently
  uncompletable — either award that XP on assignment or swap the quest).

## 4. Planning / Tax depth
Push the Planning OS roadmap further — Conflict Engine, Master Life Roadmap, Forecast Simulator
(see project_planning_os_roadmap / project_planning_os_20) — and/or deepen the Tax Center.
Largest scope; tackle after the above.
